# AUTO-GENERATED by generate.py - Op parser cases for pte_inspector.py
# Copy this into pte_inspector.py's parse_op_node function

# Add to imports at top of parse_op_node:
from executorch.backends.apple.mlx.serialization._generated.mlx_delegate import (
    ARangeNode, AddNode, AddScalarNode, ArgmaxNode, CastNode, ConcatNode, ContiguousNode, Conv1DNode, ExpandDimsNode, FullNode, GatherNode, GeluNode, IdCopyNode, ItemIntNode, LayerNormNode, LinearNode, MulNode, NoopNode, OnesNode, QuantizedGatherNode, QuantizedLinearNode, RMSNormNode, ReshapeNode, RopeNode, SdpaNode, SiluNode, SliceNode, SliceUpdateNode, SymSizeNode, TakeAlongAxisNode, TileNode, TransposeNode, ZerosNode
)

# Replace the large if/elif chain in parse_op_node with:
        # Parse based on op type (auto-generated)
        if op_name == "NoopNode":
            pass  # NoopNode has no fields
        elif op_name == "ARangeNode":
            node = init_node(ARangeNode.ARangeNode)
            result["out"] = tid(node.Out())
            result["start"] = node.Start()
            result["stop"] = node.Stop()
            result["step"] = node.Step()
            result["dtype"] = node.Dtype()
        elif op_name == "AddNode":
            node = init_node(AddNode.AddNode)
            result["a"] = tid(node.A())
            result["b"] = tid(node.B())
            result["out"] = tid(node.Out())
        elif op_name == "AddScalarNode":
            node = init_node(AddScalarNode.AddScalarNode)
            result["a"] = int_or_vid(node.A())
            result["b"] = int_or_vid(node.B())
            result["out"] = vid(node.Out())
        elif op_name == "ArgmaxNode":
            node = init_node(ArgmaxNode.ArgmaxNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["axis"] = node.Axis()
        elif op_name == "CastNode":
            node = init_node(CastNode.CastNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["dtype"] = node.Dtype()
        elif op_name == "ConcatNode":
            node = init_node(ConcatNode.ConcatNode)
            result["a"] = tid(node.A())
            result["b"] = tid(node.B())
            result["out"] = tid(node.Out())
            result["axis"] = node.Axis()
        elif op_name == "ContiguousNode":
            node = init_node(ContiguousNode.ContiguousNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
        elif op_name == "Conv1DNode":
            node = init_node(Conv1DNode.Conv1DNode)
            result["x"] = tid(node.X())
            result["w"] = tid(node.W())
            result["out"] = tid(node.Out())
            result["stride"] = node.Stride()
            result["padding"] = node.Padding()
            result["dilation"] = node.Dilation()
            result["groups"] = node.Groups()
        elif op_name == "ExpandDimsNode":
            node = init_node(ExpandDimsNode.ExpandDimsNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["axis"] = node.Axis()
        elif op_name == "FullNode":
            node = init_node(FullNode.FullNode)
            result["out"] = tid(node.Out())
            result["shape"] = [node.Shape(i) for i in range(node.ShapeLength())]
            result["v"] = node.V()
            result["dtype"] = node.Dtype()
        elif op_name == "GatherNode":
            node = init_node(GatherNode.GatherNode)
            result["table"] = tid(node.Table_())
            result["ids"] = tid(node.Ids())
            result["out"] = tid(node.Out())
        elif op_name == "GeluNode":
            node = init_node(GeluNode.GeluNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
        elif op_name == "IdCopyNode":
            node = init_node(IdCopyNode.IdCopyNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
        elif op_name == "ItemIntNode":
            node = init_node(ItemIntNode.ItemIntNode)
            result["x"] = tid(node.X())
            result["out"] = vid(node.Out())
        elif op_name == "LayerNormNode":
            node = init_node(LayerNormNode.LayerNormNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["weight"] = tid(node.Weight())
            result["bias"] = tid(node.Bias())
            result["eps"] = node.Eps()
        elif op_name == "LinearNode":
            node = init_node(LinearNode.LinearNode)
            result["x"] = tid(node.X())
            result["weight"] = tid(node.Weight())
            result["out"] = tid(node.Out())
            result["bias"] = tid(node.Bias())
        elif op_name == "MulNode":
            node = init_node(MulNode.MulNode)
            result["a"] = tid(node.A())
            result["b"] = tid(node.B())
            result["out"] = tid(node.Out())
        elif op_name == "OnesNode":
            node = init_node(OnesNode.OnesNode)
            result["out"] = tid(node.Out())
            result["shape"] = [node.Shape(i) for i in range(node.ShapeLength())]
            result["dtype"] = node.Dtype()
        elif op_name == "QuantizedGatherNode":
            node = init_node(QuantizedGatherNode.QuantizedGatherNode)
            result["table_q"] = tid(node.TableQ())
            result["scales"] = tid(node.Scales())
            result["ids"] = tid(node.Ids())
            result["out"] = tid(node.Out())
            result["biases"] = tid(node.Biases())
            result["group_size"] = node.GroupSize()
            result["bits"] = node.Bits()
            result["mode"] = node.Mode().decode("utf-8") if node.Mode() else None
            result["out_dtype"] = node.OutDtype()
        elif op_name == "QuantizedLinearNode":
            node = init_node(QuantizedLinearNode.QuantizedLinearNode)
            result["x"] = tid(node.X())
            result["w"] = tid(node.W())
            result["scales"] = tid(node.Scales())
            result["out"] = tid(node.Out())
            result["biases"] = tid(node.Biases())
            result["bias"] = tid(node.Bias())
            result["group_size"] = node.GroupSize()
            result["bits"] = node.Bits()
            result["mode"] = node.Mode().decode("utf-8") if node.Mode() else None
            result["out_dtype"] = node.OutDtype()
        elif op_name == "RMSNormNode":
            node = init_node(RMSNormNode.RMSNormNode)
            result["x"] = tid(node.X())
            result["weight"] = tid(node.Weight())
            result["out"] = tid(node.Out())
            result["eps"] = node.Eps()
        elif op_name == "ReshapeNode":
            node = init_node(ReshapeNode.ReshapeNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["shape"] = [int_or_vid(node.Shape(i)) for i in range(node.ShapeLength())]
        elif op_name == "RopeNode":
            node = init_node(RopeNode.RopeNode)
            result["q_in"] = tid(node.QIn())
            result["k_in"] = tid(node.KIn())
            result["q_out"] = tid(node.QOut())
            result["k_out"] = tid(node.KOut())
            result["head_dim"] = node.HeadDim()
            result["pos"] = vid(node.Pos())
            result["freqs"] = tid(node.Freqs())
            result["traditional"] = node.Traditional()
            result["base"] = node.Base()
            result["scale"] = node.Scale()
        elif op_name == "SdpaNode":
            node = init_node(SdpaNode.SdpaNode)
            result["q"] = tid(node.Q())
            result["k"] = tid(node.K())
            result["v"] = tid(node.V())
            result["out"] = tid(node.Out())
            result["scale"] = node.Scale()
            result["mask"] = tid(node.Mask())
            result["causal"] = node.Causal()
        elif op_name == "SiluNode":
            node = init_node(SiluNode.SiluNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
        elif op_name == "SliceNode":
            node = init_node(SliceNode.SliceNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["axis"] = int_or_vid(node.Axis())
            result["start"] = int_or_vid(node.Start())
            result["end"] = int_or_vid(node.End())
        elif op_name == "SliceUpdateNode":
            node = init_node(SliceUpdateNode.SliceUpdateNode)
            result["dst"] = tid(node.Dst())
            result["update"] = tid(node.Update())
            result["axis"] = int_or_vid(node.Axis())
            result["start"] = int_or_vid(node.Start())
            result["stop"] = int_or_vid(node.Stop())
        elif op_name == "SymSizeNode":
            node = init_node(SymSizeNode.SymSizeNode)
            result["a"] = tid(node.A())
            result["dim"] = node.Dim()
            result["out"] = vid(node.Out())
        elif op_name == "TakeAlongAxisNode":
            node = init_node(TakeAlongAxisNode.TakeAlongAxisNode)
            result["x"] = tid(node.X())
            result["indices"] = tid(node.Indices())
            result["out"] = tid(node.Out())
            result["axis"] = node.Axis()
        elif op_name == "TileNode":
            node = init_node(TileNode.TileNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["reps"] = [node.Reps(i) for i in range(node.RepsLength())]
        elif op_name == "TransposeNode":
            node = init_node(TransposeNode.TransposeNode)
            result["x"] = tid(node.X())
            result["out"] = tid(node.Out())
            result["perm"] = [node.Perm(i) for i in range(node.PermLength())]
        elif op_name == "ZerosNode":
            node = init_node(ZerosNode.ZerosNode)
            result["out"] = tid(node.Out())
            result["shape"] = [node.Shape(i) for i in range(node.ShapeLength())]
            result["dtype"] = node.Dtype()
