# Copyright (c) Meta Platforms, Inc. and affiliates.
#
# This yaml file contains operators that are also defined by the ATen library.
# For lean mode:
#   - Codegen'd target `executorch_generated_lib` will be reading all the information
#     from this file, including operator schema and kernel metadata.
#   - Selective build target `codegen:executorch_defined_ops` now is selecting all the
#     operators in this file, by dumping all the op names into `selected_operators.yaml`.
#
# For ATen mode:
#   - Codegen'd tagret `executorch_generated_lib` will read information from
#     `native_functions.yaml` in ATen library, for both operator schema and kernel metadat.
#   - Selective build will also look for operator names listed in this file and use them to
#     filter the entries in `native_functions.yaml`. Kernel metadata defined in this yaml
#     will be ignored.
#
# See the README.md file in this directory for a description of the syntax used
# by this file.

- op: _log_softmax.out
  dispatch:
    CPU: torch::executor::log_softmax_out

- op: _softmax.out
  dispatch:
    CPU: torch::executor::softmax_out

- op: _to_copy.out
  dispatch:
    CPU: torch::executor::to_copy_out

- op: abs.out
  dispatch:
    CPU: torch::executor::abs_out

- op: acos.out
  dispatch:
    CPU: torch::executor::acos_out

- op: acosh.out
  dispatch:
    CPU: torch::executor::acosh_out

- op: add.out
  dispatch:
    CPU: torch::executor::add_out

- op: addmm.out
  dispatch:
    CPU: torch::executor::addmm_out

- op: amax.out
  dispatch:
    CPU: torch::executor::amax_out

- op: amin.out
  dispatch:
    CPU: torch::executor::amin_out

- op: any.all_out
  dispatch:
    CPU: torch::executor::any_all_out

- op: arange.out
  dispatch:
    CPU: torch::executor::arange_out

- op: arange.start_out
  dispatch:
    CPU: torch::executor::arange_start_out

- op: argmax.out
  dispatch:
    CPU: torch::executor::argmax_out

- op: argmin.out
  dispatch:
    CPU: torch::executor::argmin_out

- op: as_strided_copy.out
  dispatch:
    CPU: torch::executor::as_strided_copy_out

- op: asin.out
  dispatch:
    CPU: torch::executor::asin_out

- op: asinh.out
  dispatch:
    CPU: torch::executor::asinh_out

- op: atan.out
  dispatch:
    CPU: torch::executor::atan_out

- op: atanh.out
  dispatch:
    CPU: torch::executor::atanh_out

- op: bitwise_and.Scalar_out
  dispatch:
    CPU: torch::executor::bitwise_and_Scalar_out

- op: bitwise_and.Tensor_out
  dispatch:
    CPU: torch::executor::bitwise_and_Tensor_out

- op: bitwise_not.out
  dispatch:
    CPU: torch::executor::bitwise_not_out

- op: bitwise_or.Scalar_out
  dispatch:
    CPU: torch::executor::bitwise_or_Scalar_out

- op: bitwise_or.Tensor_out
  dispatch:
    CPU: torch::executor::bitwise_or_Tensor_out

- op: bitwise_xor.Scalar_out
  dispatch:
    CPU: torch::executor::bitwise_xor_Scalar_out

- op: bitwise_xor.Tensor_out
  dispatch:
    CPU: torch::executor::bitwise_xor_Tensor_out

- op: bmm.out
  dispatch:
    CPU: torch::executor::bmm_out

- op: cat.out
  dispatch:
    CPU: torch::executor::cat_out

- op: clamp.out
  cpp_no_default_args: ['min']
  dispatch:
    CPU: torch::executor::clamp_out

- op: clone.out
  dispatch:
    CPU: torch::executor::clone_out

- op: constant_pad_nd.out
  dispatch:
    CPU: torch::executor::constant_pad_nd_out

- op: convolution.out
  dispatch:
    CPU: torch::executor::convolution_out

- op: copy.out
  dispatch:
    CPU: torch::executor::copy_out

- op: cos.out
  dispatch:
    CPU: torch::executor::cos_out

- op: cosh.out
  dispatch:
    CPU: torch::executor::cosh_out

- op: cumsum.out
  dispatch:
    CPU: torch::executor::cumsum_out

- op: detach_copy.out
  dispatch:
    CPU: torch::executor::detach_copy_out

- op: div.out
  dispatch:
    CPU: torch::executor::div_out

- op: embedding.out
  dispatch:
    CPU: torch::executor::embedding_out

- op: eq.Scalar_out
  dispatch:
    CPU: torch::executor::eq_scalar_out

- op: erf.out
  dispatch:
    CPU: torch::executor::erf_out

- op: exp.out
  dispatch:
    CPU: torch::executor::exp_out

- op: expand_copy.out
  dispatch:
    CPU: torch::executor::expand_copy_out

- op: fill.Scalar_out
  dispatch:
    CPU: torch::executor::fill_scalar_out

- op: fill.Tensor_out
  dispatch:
    CPU: torch::executor::fill_tensor_out

- op: floor.out
  dispatch:
    CPU: torch::executor::floor_out

- op: floor_divide.out
  dispatch:
    CPU: torch::executor::floor_divide_out

- op: fmod.Tensor_out
  dispatch:
    CPU: torch::executor::fmod_Tensor_out

- op: fmod.Scalar_out
  dispatch:
    CPU: torch::executor::fmod_Scalar_out

- op: full.out
  dispatch:
    CPU: torch::executor::full_out

- op: full_like.out
  dispatch:
    CPU: torch::executor::full_like_out

- op: ge.Scalar_out
  dispatch:
    CPU: torch::executor::ge_scalar_out

- op: ge.Tensor_out
  dispatch:
    CPU: torch::executor::ge_tensor_out

- op: gelu.out
  dispatch:
    CPU: torch::executor::gelu_out

- op: glu.out
  dispatch:
    CPU: torch::executor::glu_out

- op: gt.Scalar_out
  dispatch:
    CPU: torch::executor::gt_scalar_out

- op: gt.Tensor_out
  dispatch:
    CPU: torch::executor::gt_tensor_out

- op: hardtanh.out
  dispatch:
    CPU: torch::executor::hardtanh_out

- op: index.Tensor_out
  dispatch:
    CPU: torch::executor::index_Tensor_out

- op: index_put.out
  dispatch:
    CPU: torch::executor::index_put_out

- op: index_select.out
  dispatch:
    CPU: torch::executor::index_select_out

- op: isinf.out
  dispatch:
    CPU: torch::executor::isinf_out

- op: isnan.out
  dispatch:
    CPU: torch::executor::isnan_out

- op: le.Scalar_out
  dispatch:
    CPU: torch::executor::le_scalar_out

- op: le.Tensor_out
  dispatch:
    CPU: torch::executor::le_tensor_out

- op: leaky_relu.out
  dispatch:
    CPU: torch::executor::leaky_relu_out

- op: lift_fresh_copy.out
  dispatch:
    CPU: torch::executor::lift_fresh_copy_out

- op: log.out
  dispatch:
    CPU: torch::executor::log_out

- op: logical_and.out
  dispatch:
    CPU: torch::executor::logical_and_out

- op: logical_not.out
  dispatch:
    CPU: torch::executor::logical_not_out

- op: logical_or.out
  dispatch:
    CPU: torch::executor::logical_or_out

- op: logical_xor.out
  dispatch:
    CPU: torch::executor::logical_xor_out

- op: logit.out
  dispatch:
    CPU: torch::executor::logit_out

- op: lt.Scalar_out
  dispatch:
    CPU: torch::executor::lt_scalar_out

- op: lt.Tensor_out
  dispatch:
    CPU: torch::executor::lt_tensor_out

- op: masked_fill.Scalar_out
  dispatch:
    CPU: torch::executor::masked_fill_scalar_out

- op: max.dim_max
  dispatch:
    CPU: torch::executor::max_out

- op: mean.out
  dispatch:
    CPU: torch::executor::mean_dim_out

- op: min.dim_min
  dispatch:
    CPU: torch::executor::min_out

- op: minimum.out
  dispatch:
    CPU: torch::executor::minimum_out

- op: mm.out
  dispatch:
    CPU: torch::executor::mm_out

- op: mul.out
  dispatch:
    CPU: torch::executor::mul_out

- op: native_layer_norm.out
  dispatch:
    CPU: torch::executor::native_layer_norm_out

- op: ne.Scalar_out
  dispatch:
    CPU: torch::executor::ne_scalar_out

- op: ne.Tensor_out
  dispatch:
    CPU: torch::executor::ne_tensor_out

- op: neg.out
  dispatch:
    CPU: torch::executor::neg_out

- op: nonzero.out
  dispatch:
    CPU: torch::executor::nonzero_out

- op: ones.out
  dispatch:
    CPU: torch::executor::ones_out

- op: permute_copy.out
  dispatch:
    CPU: torch::executor::permute_copy_out

- op: pow.Tensor_Scalar_out
  dispatch:
    CPU: torch::executor::pow_Tensor_Scalar_out

- op: pow.Tensor_Tensor_out
  dispatch:
    CPU: torch::executor::pow_Tensor_Tensor_out

- op: reciprocal.out
  dispatch:
    CPU: torch::executor::reciprocal_out

- op: relu.out
  dispatch:
    CPU: torch::executor::relu_out

- op: remainder.Tensor_out
  dispatch:
    CPU: torch::executor::remainder_Tensor_out

- op: remainder.Scalar_out
  dispatch:
    CPU: torch::executor::remainder_Scalar_out

- op: repeat.out
  dispatch:
    CPU: torch::executor::repeat_out

- op: round.out
  dispatch:
    CPU: torch::executor::round_out

- op: rsqrt.out
  dispatch:
    CPU: torch::executor::rsqrt_out

- op: rsub.Scalar_out
  dispatch:
    CPU: torch::executor::rsub_scalar_out

- op: scalar_tensor.out
  dispatch:
    CPU: torch::executor::scalar_tensor_out

- op: scatter_add.out
  dispatch:
    CPU: torch::executor::scatter_add_out

- op: select_copy.int_out
  dispatch:
    CPU: torch::executor::select_copy_int_out

- op: select_scatter.out
  dispatch:
    CPU: torch::executor::select_scatter_out

- op: sigmoid.out
  dispatch:
    CPU: torch::executor::sigmoid_out

- op: sign.out
  dispatch:
    CPU: torch::executor::sign_out

- op: sin.out
  dispatch:
    CPU: torch::executor::sin_out

- op: sinh.out
  dispatch:
    CPU: torch::executor::sinh_out

- op: slice_copy.Tensor_out
  dispatch:
    CPU: torch::executor::slice_copy_Tensor_out

- op: slice_scatter.out
  dispatch:
    CPU: torch::executor::slice_scatter_out

- op: split_copy.Tensor_out
  dispatch:
    CPU: torch::executor::split_copy_Tensor_out

- op: sqrt.out
  dispatch:
    CPU: torch::executor::sqrt_out

- op: squeeze_copy.dim_out
  dispatch:
    CPU: torch::executor::squeeze_copy_dim_out

- op: stack.out
  dispatch:
    CPU: torch::executor::stack_out

- op: sub.out
  dispatch:
    CPU: torch::executor::sub_out

- op: sum.IntList_out
  dispatch:
    CPU: torch::executor::sum_dim_out

- op: t_copy.out
  dispatch:
    CPU: torch::executor::t_copy_out

- op: tan.out
  dispatch:
    CPU: torch::executor::tan_out

- op: tanh.out
  dispatch:
    CPU: torch::executor::tanh_out

- op: transpose_copy.int_out
  dispatch:
    CPU: torch::executor::transpose_copy_int_out

- op: tril.out
  dispatch:
    CPU: torch::executor::tril_out

- op: unbind_copy.int_out
  dispatch:
    CPU: torch::executor::unbind_copy_int_out

- op: unsqueeze_copy.out
  dispatch:
    CPU: torch::executor::unsqueeze_copy_out

- op: var.out
  dispatch:
    CPU: torch::executor::var_out

- op: view_copy.out
  dispatch:
    CPU: torch::executor::view_copy_out

- op: where.self_out
  dispatch:
    CPU: torch::executor::where_out

- op: zeros.out
  dispatch:
    CPU: torch::executor::zeros_out
