name: pull

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}-${{ github.event_name == 'schedule' }}
  cancel-in-progress: true

jobs:
  buck-build-test:
    name: buck-build-test
    uses: pytorch/test-infra/.github/workflows/linux_job.yml@use-shareable-gha-calculate-docker-image
    with:
      runner: linux.2xlarge
      docker-image: executorch-ubuntu-22.04-clang12
      fetch-depth: 0
      ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      script: |
        # TODO: A placeholder for future build and test steps
        echo "Thank You Mario, But Our Princess is in Another Castle"
        sleep 300
