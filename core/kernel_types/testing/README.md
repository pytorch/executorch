This directory contains code to help tests that use the types defined in
`//executorch/core/kernel_types`. This code should be treated as test-only, and
should not be compiled into production targets.
